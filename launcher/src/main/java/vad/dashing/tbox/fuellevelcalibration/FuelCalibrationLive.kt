package vad.dashing.tbox.fuellevelcalibration

/** Stub — fuel calibration is Monitor-only. */
object FuelCalibrationLive {
    fun applyFromStableFilteredPercent(percent: UInt) {
        // no-op in standalone launcher
    }
}
